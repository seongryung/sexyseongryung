#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX 12 
/*
void main() {
	int a[MAX];
	for (int i = 0; i < MAX; i++) {
		a[i] = 0;
	}
	int number;
	int k;
	
	printf("2진수로 변환할 숫자를 입력해 주세요(양의정수) : ");
	scanf("%d", &number);

	
	for (int b = MAX-1; b >= 0; b--) {
		if (number == 0) {
			break;
		}

		a[b] = number % 2;
		number = number / 2;
		
	}
	
	for (k = 0; k < MAX; k++) {
		printf("%d", a[k]);
	}
	printf("\n\n");
	
}
*/