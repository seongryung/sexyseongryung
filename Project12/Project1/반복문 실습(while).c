#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include <stdlib.h>
#include <time.h>

//void main() {
	
	/*
	srand(time(NULL));
	int randNum = 1 + rand() % 50; //1~50 사이의 난수 생성
	printf("컴퓨터의 수 : %d\n", randNum);
	int number;
	while (1) {
		printf("input : ");
		scanf("%d", &number);
		if (number == randNum) {
			printf("ㅇㅋ\n");
			return 0;
		}
		if (number >= randNum) {
			printf("Down\n");
		}
		if (number <= randNum) {
			printf("Up\n");
		}
			

	}
	*/






//}
